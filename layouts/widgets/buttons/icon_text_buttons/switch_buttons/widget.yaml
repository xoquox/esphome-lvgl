# required vars:
# - uid
# - entity_id
# - width
# - height
# - text

# button:
  <<: !include
    file: ../stateful.yaml
    vars:
      uid: ${uid}
      entity_id: ${entity_id}
      width: ${width}
      height: ${height}
      icon_on: "\U000F1253"
      icon_off: "\U000F1254"
      text: ${text}
  on_click:
    - homeassistant.service:
        service: switch.toggle
        data:
          entity_id: ${entity_id}
